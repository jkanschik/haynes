<%= render :partial => '/search/record_navigation', :locals => {:next_action => :edit} %>

<h1>Editing reference information for <%= @quotation.label %></h1>

<% form_tag :action => 'update', :id => @quotation, :search_id => params[:search_id] do %>
  <%= render :partial => 'form' %>
  <%= submit_tag 'Save', :class => 'button' %>
  <%= submit_tag 'Cancel', :name => 'cancel', :class => 'button'%>
<% end %>
